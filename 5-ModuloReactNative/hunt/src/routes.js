import { createStackNavigator } from 'react-navigation-stack' //Pesquisar no site da rocketseat sobre react-navigation

import Main from './pages/main'

export default createStackNavigator({
    Main 
})
